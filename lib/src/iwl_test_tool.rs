use crate::adapters::{CsiDataAdapter, DataAdapterConfig};
use crate::adapters::iwl::*;
use crate::handler::*;
use crate::sinks::*;
use crate::sources::*;
use crate::FromConfig;



fn main() -> Result<(), Box<dyn Error>> {
    Ok(())
}